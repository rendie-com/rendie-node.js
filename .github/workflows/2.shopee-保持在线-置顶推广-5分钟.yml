name: 2.shopee-保持在线-置顶推广-5分钟

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write
  actions: write

env:   
  TZ: Asia/Shanghai
  NODE_USERNAME: "5minute"
  NODE_ACCESS_TOKEN: ${{ secrets.NODE_ACCESS_TOKEN_2 }}
  MAX_RUNTIME_MINUTES: 3
  GITHUB_TOKEN: ${{ secrets.MY_PAT }}
  TARGET_DIR: "error/task2"
  TARGET_URL: "https://www.rendie.com/admin"
  TEMPLATE: "Shopee/定时任务/index.js&jsFile=js02"

jobs:
  automation_job:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.60.0-jammy
    timeout-minutes: 5
    defaults:
      run:
        working-directory: ./playwright # 保持你明确的目录结构

    steps:
      - name: 1. Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: 2. Setup Node.js (v24)
        uses: actions/setup-node@v4 # 升级到稳定大版本 v4，并对齐 Node 24
        with:
          node-version: 24
          cache: 'npm'
          cache-dependency-path: './playwright/package-lock.json'

      - name: 3. Execution
        run: |
          npm ci --prefer-offline --no-audit --no-fund --omit=optional --quiet
          npm start